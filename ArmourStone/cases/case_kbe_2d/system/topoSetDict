FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      topoSetDict;
}

actions
(
    {
        name    propellerSet;
        type    cellSet;
        action  new;
        source  boxToCell;
        box     (19.6 0 4.5)
                (20.4 0.1 5.5);
    }

    {
        name    propellerZone;
        type    cellZoneSet;
        action  new;
        source  setToCellZone;
        set     propellerSet;
    }
);
